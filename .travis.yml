language: python

python:
  - 3.6

addons:
  postgresql: "9.4"

services:
  - postgresql

env:
  - DJANGO=2.1.7

install:
  - pip install -r requirements.txt

before_script:
  # - psql -c 'create database ah-the-jedi;' -U postgres

script:
  - python manage.py makemigrations
  - python manage.py migrate
  - python manage.py test
